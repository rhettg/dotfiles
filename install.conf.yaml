- create:
    ~/.ssh:
      mode: 0700
- link:
    ~/.bashrc:
        force: true
        path: bashrc
    ~/.bash_profile: bash_profile
    ~/.bash_prompt: bash_prompt
    ~/.exports: exports
    ~/.aliases: aliases
    ~/.path: path
    ~/.vimrc: vimrc
    ~/.dircolors: dircolors
- shell:
    - [touch ~/.hushlogin]